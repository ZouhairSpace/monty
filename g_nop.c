#include "monty.h"

/**
 * f_nop - no operation (does nothing).
 * @head: pointer to the head of the stack
 * @counter: line number
 * Return: no return
 */
void f_nop(stack_t **head, unsigned int counter)
{
	(void)counter; /* Unused parameter counter */
	(void)head; /* Unused parameter head */
}
